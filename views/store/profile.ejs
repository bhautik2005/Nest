<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile</title>
    <link
        href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600&family=DM+Sans:wght@300;400;500&display=swap"
        rel="stylesheet">
    <style>
        *,
        *::before,
        *::after {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        :root {
            --coral: #FF385C;
            --coral-dark: #E31C5F;
            --charcoal: #222222;
            --mid: #717171;
            --border: #DDDDDD;
            --bg: #F7F7F7;
            --white: #FFFFFF;
            --shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
        }

        body {
            font-family: 'DM Sans', sans-serif;
            background: var(--bg);
            color: var(--charcoal);
            min-height: 100vh;
        }

        /* ── HERO BANNER ── */
        .profile-hero {

            background: #a8c0ff;
            /* fallback for old browsers */
            background: -webkit-linear-gradient(to right, #3f2b96, #a8c0ff);
            /* Chrome 10-25, Safari 5.1-6 */
            background: linear-gradient(to right, #3f2b96, #a8c0ff);
            /* W3C, IE 10+/ Edge, Firefox 16+, Chrome 26+, Opera 12+, Safari 7+ */

            padding: 48px 24px 80px;
            position: relative;
            overflow: hidden;
        }

        .profile-hero::before {
            content: '';
            position: absolute;
            inset: 0;

        }

        .profile-hero-inner {
            max-width: 900px;
            margin: 0 auto;
            display: flex;
            align-items: center;
            gap: 28px;
            position: relative;
            z-index: 1;
        }

        .avatar-wrap {
            position: relative;
            flex-shrink: 0;
        }

        .avatar {
            width: 96px;
            height: 96px;
            border-radius: 50%;
            background: var(--white);
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Playfair Display', serif;
            font-size: 36px;
            font-weight: 600;
            color: var(--coral);
            border: 4px solid rgba(255, 255, 255, 0.4);
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
            text-transform: uppercase;
        }

        .verified-badge {
            position: absolute;
            bottom: 2px;
            right: 2px;
            background: #00A699;
            color: #fff;
            border-radius: 50%;
            width: 26px;
            height: 26px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 13px;
            border: 2px solid #fff;
        }

        .hero-info {
            min-width: 0;
        }

        .hero-info h1 {
            font-family: 'Playfair Display', serif;
            font-size: 1.9rem;
            color: #fff;
            margin-bottom: 6px;
        }

        .hero-info .email-badge {
            display: inline-block;
            background: rgba(255, 255, 255, 0.18);
            color: rgba(255, 255, 255, 0.9);
            font-size: 0.88rem;
            padding: 3px 12px;
            border-radius: 20px;
            backdrop-filter: blur(4px);
            margin-bottom: 10px;
        }

        .hero-info .usertype-row {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .hero-info .usertype {
            background: rgba(255, 255, 255, 0.15);
            color: #fff;
            font-size: 0.82rem;
            font-weight: 500;
            padding: 4px 14px;
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .hero-info .member-tag {
            color: rgba(255, 255, 255, 0.65);
            font-size: 0.8rem;
        }

        /* ── MAIN ── */
        .main {
            max-width: 900px;
            margin: -40px auto 60px;
            padding: 0 24px;
        }

        /* ── CARDS GRID — always 3 columns ── */
        .cards-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 18px;
            margin-bottom: 36px;
             padding-top:63px;
        }

        @media (max-width: 680px) {
            .cards-grid {
                grid-template-columns: 1fr;
            }

            .profile-hero-inner {
                flex-direction: column;
                text-align: center;
            }

            .hero-info .usertype-row {
                justify-content: center;
            }
        }

        .card {
            background: var(--white);
            border-radius: 20px;
            box-shadow: var(--shadow);
            overflow: hidden;
            text-decoration: none;
            color: inherit;
            display: block;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 14px 36px rgba(0, 0, 0, 0.13);
        }

        .card-top {
            padding: 26px 22px 18px;
            border-bottom: 1px solid var(--border);
        }

        .card-icon {
            width: 50px;
            height: 50px;
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            margin-bottom: 14px;
        }

        .icon-fav {
            background: #FFF0F3;
        }

        .icon-book {
            background: #F0FFF9;
        }

        .icon-home {
            background: #FFF8E7;
        }

        .card-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--charcoal);
        }

        .card-bottom {
            padding: 14px 22px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            font-size: 0.83rem;
            color: var(--coral);
            font-weight: 500;
        }

        .card-bottom .arrow {
            font-size: 18px;
        }
    </style>
</head>

<body>

    <!-- Nav partial already has its own styles/positioning -->
    <header>
        <%- include('../partials/nav') %>
    </header>

    <!-- HERO — starts right below the nav -->
    <div class="profile-hero">
        <div class="profile-hero-inner">

            <div class="avatar-wrap">
                <div class="avatar">
                    <%= (user.firstName && user.firstName[0]) || user.email[0] %>
                </div>
                <div class="verified-badge">✓</div>
            </div>

            <div class="hero-info">
                <h1>
                    <%= user.firstName %>
                        <%= user.lastName || '' %>
                </h1>
                <div class="email-badge">✉ <%= user.email %>
                </div>
                <div class="usertype-row">
                    <span class="usertype">🏠 <%= user.userType %></span>
                    <span class="member-tag">· Member</span>
                </div>
            </div>

        </div>
    </div>

    <!-- MAIN CONTENT -->
    <div class="main">

        <!-- 3 Quick-link cards -->
        <div class="cards-grid">

            <a href="/store/favourite" class="card">
                <div class="card-top">
                    <div class="card-icon icon-fav">❤️</div>
                    <div class="card-title">My Favourites</div>
                </div>
                <div class="card-bottom">View Favourites <span class="arrow">→</span></div>
            </a>

            <a href="/store/booking" class="card">
                <div class="card-top">
                    <div class="card-icon icon-book">🗓️</div>
                    <div class="card-title">My Bookings</div>
                </div>
                <div class="card-bottom">View Bookings <span class="arrow">→</span></div>
            </a>

            <a href="/store/homelist" class="card">
                <div class="card-top">
                    <div class="card-icon icon-home">🏡</div>
                    <div class="card-title">Home Listings</div>
                </div>
                <div class="card-bottom">Explore Homes <span class="arrow">→</span></div>
            </a>

        </div>


    </div>



</body>

</html>